/** Shared Warm Concrete wrapper for legacy gym pages. */
export default function WarmConcrete({ children }: { children: React.ReactNode }) {
  return <div className="warm-concrete">{children}</div>
}
