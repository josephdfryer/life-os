{
  "name": "@life-os/automation",
  "description": "Shared rules engine - triggers, conditions, and actions for automation.",
  "version": "0.0.1",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "dependencies": {
    "@life-os/access": "*",
    "@life-os/contracts": "*",
    "@life-os/db": "*",
    "@life-os/domain": "*"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  },
  "scripts": {
    "test": "tsx --test tests/*.test.ts",
    "type-check": "tsc --noEmit --moduleResolution bundler --module esnext --target es2022 --strict index.ts triggers.ts conditions.ts actions.ts rules.ts"
  }
}
