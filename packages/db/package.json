{
  "name": "@life-os/db",
  "description": "Prisma client, generated types, and database crypto helpers.",
  "version": "0.0.1",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./crypto": "./src/crypto.ts",
    "./enrich": "./src/enrich.ts",
    "./testing": "./testing.ts"
  },
  "scripts": {
    "build": "npm run generate",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "repair:workspace-membership": "tsx repair-workspace-membership.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.10.0",
    "@prisma/client": "^7.7.0",
    "dotenv": "^17.4.1",
    "pg": "^8.23.0",
    "prisma": "^7.7.0"
  }
}
